[env]
TERM = "xterm-256color"

[terminal]
shell = { program = "/usr/local/bin/zellij" }

[window]
padding.x = 10
padding.y = 10
decorations = "Buttonless"
blur = true
opacity = 0.9
option_as_alt = "Both"

[keyboard]
bindings = [{ key = "F11", action = "ToggleFullscreen" }]
